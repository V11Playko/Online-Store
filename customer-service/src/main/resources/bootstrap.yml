#Application Configuration
spring:
  application:
    name: customer-service

  cloud:
    config:
      uri: http://localhost:8081
      username: root
      password: s3cr3t
      profile: dev
  boot:
    admin:
      client:
        url: http://localhost:8086